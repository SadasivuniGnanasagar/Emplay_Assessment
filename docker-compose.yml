version: '3'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    depends_on:
      - redis

  frontend:
    build: ./frontend
    ports:
      - "4200:4200"

  redis:
    image: redis:7
    ports:
      - "6379:6379"